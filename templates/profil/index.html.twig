{% extends 'base.html.twig' %}

{% block title %}Profil utilisateur
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
	<div class="wrapper">
		<div class="d-flex justify-content-center">
			<h1 class=>Voici vos informations</h1>
		</div>
		<div class="d-flex flex-column align-items-center">
			<br>
			<br>
			<h2>Votre identifiant</h2>
			<h2>{{user.email}}</h2>
			<br>
			<h2>Votre rôle</h2>
			<h2>{{user.roles | join(" ,")}}</h2>
			<br>
			<div>
				<a href="{{ path('profil_update', {'id': user.id}) }}" class="btn btn-success">Modifier vos informations et votre mot de passe</a>
			</div>
		</div>

	</div>


{% endblock %}
